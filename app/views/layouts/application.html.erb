<!DOCTYPE html>
<html>
<head>
  <title>App2</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="row"> 
      <div class="navbar-left">
        <%= image_tag 'logo.png' %>
      </div>

      <div class="navbar-right">
        <% if user_signed_in? %>
        <p>Signed in as <%= current_user.email %>.
        <%= link_to "Sign out", destroy_user_session_path,:class => 'btn btn-default', :method => :delete %>
        <% else %>
        <%= link_to "Sign in", new_user_session_path, :class => 'btn btn-default' %>
        <% end %></p>
      </div>
    </div>
  </div><!-- end of container fluid -->
</nav>

<div class="container">
  <div class="row">
    <div class="col-md-12">
    <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %> alert-warning">
      <a href="#" data-dismiss="alert" class="close">×</a>
        <%= value %>
        </div>
    <% end %>
    </div>
  </div>

 <%= yield %>
<footer>
  <div class="container">
    <p>&copy; Copyright 2014 by MSN. All Rights Reserved.</p>
  </div>
</footer>
</div>
</body>
</html>
